# Data folder

Place the dataset CSV here:

- `mxmh_survey_results.csv`

This repo is configured to **not commit** raw data files by default (see `.gitignore`).  
If you decide to include the dataset in a public repo, verify the dataset's license/terms first.
